AI COMMIT BOUNDARY 2026-05-27 - Next commit context after latest push

Everything below this boundary belongs to the next set of changes after the user's latest push, unless a later boundary appears.

AI 2026-05-27 - AI context organization after latest push

User pushed the previous change and asked that future work separate discussion history by commit. From now on, after the user commits or pushes, the next recorded discussion in this file should start a new simple section for the next commit context.

AI communication/context files were consolidated under `Assets/AI_Context/`: `AI_Instructions.txt`, `AI_Csharp`, `AI_DiscussionHistory.txt`, and `AI_Discussions_summary.txt`. `DiscussionHistory.txt` was renamed to `AI_DiscussionHistory.txt` so the file itself carries the AI prefix. `Assets/Scripts/TO-DO.md` remains in `Assets/Scripts` because it is an architectural follow-up list, not purely an AI communication file.

AI 2026-05-26 - Combat runtime context and ticked flow

User wants CombatContext to carry live combat-wide runtime information, not only static data. This includes participating characters, available actions, active flow counts, created flow counters, and similar state used by item conditions.

Performance assumption: combat tick may run about 10 times per second, and possibly more often later. The target scale can reach roughly 1,000-2,000 active items and flows. Anything in tick must be treated as hot-path code. Avoid per-tick allocations, LINQ materialization, reflection, broad collection rebuilds, and hidden O(n*m) behavior. Prefer indexes, reusable buffers, explicit phase plans, cheap query endpoints, and bounded work budgets.

Important decision: ICombatQueries must remain a lightweight query surface. It should return simple values such as int, float, bool, string, enums, or cheap primitive-like values. It must not expose complex runtime objects, collections, wrappers, DTO graphs, or mutable domain internals.

Current concrete need: item conditions should be able to ask whether there are more than 3 active flows and decide not to create a new flow. This first needs live active flow tracking and query endpoints.

Direction for implementation: stop treating FlowProcessor as fire-and-forget async. Flow should become ticked runtime state, similar in spirit to InventoryPlacedEntryPoint ticking. Active flows should be stored by combat runtime/characters, ticked during combat ticks, and removed only after completion.

Implementation detail refined: combat tick should process phases from stable snapshots. Active flows are copied as references into a reusable buffer, ticked from that buffer, and then the active flow list is compacted. This avoids newly added flows affecting the current flow phase while also avoiding per-tick list allocation. For now, flow phase runs before item/entry-point phase, so item conditions see state after already-active flows were processed and before newly created flows are processed on a later tick.

Follow-up refinement: snapshot buffers should not live as vague fields like flowTickBuffer on CombatCharacter. Tick orchestration should be explicit as a small tick plan/action chain: first include participants for the tick, then execute named phases in order. CombatCharacter can own active runtime state, but the order and snapshot mechanics belong to CombatTickPlan.

Architectural concern: the order of combat tick phases should not be defined ad hoc inside CombatCharacter. CombatCharacter can expose or own character-local runtime state, but the global order of phases is a combat runtime rule. The next refactor should move phase ordering to a combat-level orchestrator/runner or a reusable tick pipeline, while Character supplies participants/state.

Immediate code refinement: CombatCharacter may own a CombatTickPlan-like helper, but CombatCharacter should not spell out the phase order through a chain. It should pass active flows, tickable items, character id, combat ticks, and capabilities into the plan/runner. The plan/runner owns the phase order.

IDE/performance refinement: methods that execute tick phases should not look like cheap builder calls. CombatTickPlan should separate preparation of snapshots from execution of prepared phases. Expensive phase methods should be named as execution methods, not fluent "include/build" methods.

Open point for later: decide how Duration seconds map to CombatTicks. For the first implementation step, avoid locking in a final time conversion unless needed.

Previous flow behavior before today's refactor:

- FlowProcessor exposed start() and internally launched startAsync() as fire-and-forget work.
- stepLoopAsync processed nodes asynchronously until there were no more nodes.
- For every current node, FlowProcessor prepared ExecuteActionCommand and delegated execution to IActionExecutor.
- ActionExecutorService created PreparedAction, read IActionDescription.getCastTime(), waited with TimeModule.ContinueIn(duration), and only then executed the item effects.
- The visible delay between items came mostly from each item's Duration/castTime through ActionExecutorService. Router logic only chose the next item; it did not own the delay.
- FlowProcessorSettings.maxStepsPerSlice and IFlowStepScheduler were an additional async/yield safeguard, not the main item-to-item delay.

What changed today:

- FlowProcessor no longer exposes start() and no longer owns async fire-and-forget execution.
- IFlowProcessor now exposes tick(CombatTicks) and isFinished().
- FlowProcessor applies node effects during tick and advances through routed nodes in the ticked runtime model.
- Active flows are stored on CombatCharacter for this first implementation step, counted through simple query endpoints, and removed after completion.
- CombatCharacter now reports active and created flow counts through ICharacterCombatQueries, and combat-wide ICombatQueries aggregates those counts.
- IFlowStepScheduler was removed because the current direction is ticked runtime rather than async/yielded flow processing.
- CombatTickPlan was introduced as an explicit phase plan. CombatCharacter passes participants/context to it, while the plan owns the local phase order for now.

Important consequence:

- The old castTime-based item delay is not yet represented in the new ticked model. Duration-to-CombatTicks mapping and per-flow/per-character tick work budgeting still need a deliberate design step.

AI 2026-05-26 - Combat tick performance smoke test

User wants a very early regression guard for the hot combat tick path. This should not pretend to be a reliable benchmark yet, because proper performance tests need warmup, stable runners, controlled logging, GC awareness, and eventually Unity profiling/performance-test tooling.

Immediate direction: add a simple NUnit performance smoke test using Stopwatch. It should run a real BattleSession with about 1,000 tickable entry point items and about 1,000 active flows, warm the scenario up first, then assert a small preliminary per-tick budget. The test is intended to catch accidental catastrophic regressions, not to define final production performance.

Implementation detail: use isolated entry points on a checkerboard pattern across multiple characters. This keeps the scenario focused on "many tickable items + many active flows" instead of accidentally measuring a huge connected flow graph caused by grid adjacency.

Correction after first run: the initial isolated-entry-point version was too noisy because each flow could finish quickly and enter consumeFlow/damage processing, producing a lot of Debug.Log output and causing Rider's test output truncation. The smoke test should instead create the initial flows manually, use entry points with a long trigger interval so no new flows are created during measurement, set maxStepsPerSlice to 1, and give every flow a short no-op item chain so measured ticks process active flows without finishing them.

AI COMMIT BOUNDARY 2026-05-27 - after push 125e8d6 (master)

Everything below this boundary belongs to the next set of changes after the latest push, unless a later boundary appears.

AI COMMIT BOUNDARY 2026-05-27 - after push 64685f0 (master) - Record boundary updates for durable AI/project decisions; enhance pre-push script to include commit subject context.

Everything below this boundary belongs to the next set of changes after the latest push, unless a later boundary appears.

AI COMMIT BOUNDARY 2026-05-27 - after push ef4027f (master) - Clarify commit boundary usage and guidelines; update documentation to reflect durable decisions and introduce token usage mitigation strategy.

Everything below this boundary belongs to the next set of changes after the latest push, unless a later boundary appears.

AI 2026-05-27 - Combat tick stress smoke test refinement

User clarified that the performance smoke should stress a more complex fight, not only isolate already-active flow ticking. New target for this smoke scenario: exactly 12 combat characters, exactly 5,000 equipped entry point items across those characters, exactly 5,000 initially active flow commands, and a measured run representing 10 real-time seconds at 100 combat ticks per real-time second. The first threshold should be intentionally ambitious, around 10 ms average per tick, as a catastrophic regression guard rather than a final benchmark.

The test is allowed to let entry points create new flows during measurement because that better reflects a busy fight. To keep the setup deterministic, the test should still assert the exact character count, exact placed item count, exact initial active/created flow counts, and the expected created flow count after the measured trigger waves.

Previous inventory sizing mechanism: `InventoryAggregate.create` always created a fixed 17x8 inventory. This made the stress test misleading, because the constants in the test looked like scenario scale even though 1,000+ items required silently adding many characters. First replacement made inventory dimensions globally configurable through DI, but that still implied every character had the same inventory size.

Current mechanism: global inventory config now describes only the maximum allowed grid size through `InventoryGridConfiguration`. The actual inventory size is a character characteristic carried by `CreateCombatCharacterCommand` as a `GridDimensions` value. This keeps the current 17x8 character inventories explicit while allowing stress-test characters to use 100x50 and leaving room for future character-specific/GUI-determined inventory layouts.

Previous battle tick iteration mechanism: `BattleRuntime.tick` copied all combat characters with `ToList()` every tick before iterating. New mechanism: it iterates the current `IReadOnlyCollection<ICombatCharacterFacade>` directly, avoiding that per-tick list allocation. This assumes character membership is stable during the tick; if combat can later add/remove characters mid-tick, the combat runtime should introduce an explicit reusable participant snapshot rather than LINQ materialization.

GUI performance testing is a separate concern. A later PlayMode or scene-level test can include rendered inventory/UI, but this domain smoke should stay focused on combat runtime cost so failures point to tick/flow/item logic rather than prefabs, layout, rendering, or editor UI overhead.

Correction after first stress test run: active flow count dropped from 5,000 to 4,986 before measurement because the warmup ticked already-created dense-grid flows. With single-cell entry points packed next to each other, `GridAdjacencyRouter` performs random neighbor walking and some flows can hit a local dead end quickly. The test now warms up the session before manually creating the initial 5,000 flows, so the measured setup starts deterministically with exactly 5,000 active flows while still allowing flow completion and new flow creation during measurement.

AI 2026-05-27 - Flow processor settings validation

Previous mechanism: `FlowProcessorSettings` silently clamped `maxStepsPerSlice < 1` to `1` and had a placeholder `// verify` comment. The likely intent was to protect flow ticking from a zero/negative per-tick work budget, because `FlowProcessor.tick` only advances while `stepsProcessed < maxStepsPerSlice`.

New mechanism: keep `maxStepsPerSlice` as explicit flow configuration, supplied by DI defaults or test overrides, but make invalid values fail fast with `ArgumentOutOfRangeException`. This avoids hiding bad installer/test configuration and keeps the setting object as a small validated value object rather than moving the guard into hot-path flow code.

Follow-up interpretation: the old default value `64` on `FlowProcessorSettings` was a magic number and likely came from the previous async/yielded flow loop. In the ticked runtime model, pacing should eventually come from combat ticks plus item cast time/cooldown/duration, not from "process up to N nodes immediately." `maxStepsPerSlice` remains only as a per-flow, per-tick safety/work cap that prevents one connected flow from walking an unbounded number of nodes in a single tick. The default value should therefore live in composition/game configuration, not inside the settings constructor. `FlowProcessorSettings` now requires an explicit positive value, and `FlowProcessor` no longer creates hidden fallback settings.

User prefers Java-like code shape for domain/configuration values: private fields plus explicit getters instead of C# auto-properties such as `public int value { get; }`. `FlowProcessorSettings` now stores `maxStepsPerSlice` in a private readonly field and exposes `getMaxStepsPerSlice()`. The default safety cap was raised to 5,000 so it acts as an extreme loop/graph-walk guard rather than a likely gameplay limiter for combos.

AI 2026-05-27 - C# style guidance location

User clarified that durable C# code-shape/style decisions should be recorded in `Assets/AI_Context/AI_Csharp` and followed when writing future code. Added guidance there to record future C# style decisions, prefer explicit typing, and prefer Java-like private fields plus explicit getters for domain/config/value objects while still respecting existing local code patterns.

AI 2026-05-27 - Inventory grid max versus character inventory size

User clarified that the DI-bound inventory grid value should not be the actual size of every character inventory. The global concept needed now is the maximum supported inventory grid size, partly constrained by GUI/layout capability. The actual inventory dimensions are a characteristic of the character being created.

Implementation direction: use shared `GridDimensions` as a small value object, bind `InventoryGridConfiguration` in DI with max allowed dimensions, and pass each character's actual `GridDimensions` through `CreateCombatCharacterCommand`. `CharacterInventoryFactory` validates the requested character grid against the max before creating `InventoryAggregate`.

AI 2026-05-27 - Combat tick rate and speed multiplier interpretation

User clarified tick-rate semantics: a combat tick is the smallest game time unit, not a domain concept of "one game second." The default GUI/runtime execution speed should be treated as 10 combat ticks per real-time second. Faster game speed means executing more ticks per real-time second, e.g. x10 speed means 100 ticks per real-time second.

Avoid describing mechanics as "1 game second = 10 ticks" for now. Prefer saying that a rule lasts or triggers after N ticks, while GUI/runtime speed decides how many ticks are executed per real-time second. The performance smoke test now names this explicitly with `DefaultCombatTicksPerRealSecond = 10`, `StressSpeedMultiplier = 10`, and `StressCombatTicksPerRealSecond = 100`.

Follow-up implementation: the default runtime speed now lives in `BattleSessionSettings`, owned by the battle/session layer rather than the domain tick model or only a test constant. Default is 10 combat ticks per real-time second. `BattleUIManager` uses `BattleSessionSettings.getRealSecondsPerCombatTick()` for its coroutine wait, and the performance smoke test reads the default from `BattleSessionSettings`.

AI COMMIT BOUNDARY 2026-05-28 - after push 22039a9 (master) - Introduce `BattleSessionSettings`, `GridDimensions`, and `InventoryGridConfiguration`; refactor and validate combat tick rate, inventory grid sizing, and flow processing logic. Include performance smoke test updates for combat tick scenarios and adjust related dependencies.

Everything below this boundary belongs to the next set of changes after the latest push, unless a later boundary appears.

AI 2026-05-28 - Cast time, tick semantics, validation, and random item value notes

User wants to restore/support item castTime after the previous tick refactor, while keeping combat tick performance as a critical hot path. Current concern: using raw ticks everywhere is mechanically clean but might make item behavior harder to reason about in user-friendly terms; using a separate "GameSecond" might improve readability but risks creating a second competing gameplay time unit.

Current recommendation: keep `CombatTicks` as the canonical domain/gameplay time unit and do not introduce `GameSecond` as a separate domain concept yet. If user-facing seconds are needed, derive them at UI/config boundaries from `BattleSessionSettings`. For item characteristics, prefer tick-based value objects: either `CombatTicks` directly or a small semantic wrapper such as `ItemCastTime`/`CastTime` that contains `CombatTicks` and validates invariants. Avoid `Duration(float)` for gameplay mechanics; floats should stay at presentation/Unity animation/time-boundary layers unless wrapped by a domain time object.

Cast time direction under ticked flow: flow should not process every node immediately up to `maxStepsPerSlice` as normal pacing. Each active flow should keep per-flow progress for the current node's cast time, accumulate elapsed `CombatTicks`, execute the item only when enough ticks were accumulated, then route to the next node and reset progress for that next node. `FlowProcessorSettings.maxStepsPerSlice` remains only an emergency safety cap against runaway graph walking in one tick.

User also asked to record a broader architectural rule: every object should validate data consistency. Null guards are the main current form, but the real principle is invariant validation. Domain/config/runtime objects should check nulls, ids, enum values, positive/non-negative quantities, dimensions, and coherent combinations at construction/update boundaries. Avoid putting expensive validation in hot tick loops; tick code should receive already coherent data.

Design note for later item generation/balancing: user strongly wants highly generic random items, where two identical items are extremely rare and items can have many different specifications in many orders. This makes item valuation one of the most interesting and dangerous design areas. The value model must reason contextually, not just by raw numbers. Example: very long cast time may be valuable for defensive items, while very short cast time is likely highly valuable for offensive items. This is not being solved now, but future item characteristics should be designed so contextual valuation can be added.

AI 2026-05-28 - Implemented tick-owned item cast time

Previous mechanism: item action descriptions exposed `Duration(float)`, `ActionExecutorService` read that float and waited through `TimeModule.ContinueIn(...)`, and old async flow processing delegated timing to the executor. After the earlier flow refactor this mechanism was no longer active in the ticked model, so `FlowProcessor.tick(...)` processed nodes immediately up to `FlowProcessorSettings.maxStepsPerSlice`.

New mechanism: `Duration(float)` was removed from gameplay/domain action descriptions. `IActionDescription.getCastTime()` now returns `ItemCastTime`, a small value object that wraps `CombatTicks` and rejects negative cast time. Item catalog definitions now express cast time directly in ticks. `FlowProcessor` stores `ticksUntilCurrentNodeExecution` for the current node, spends incoming `CombatTicks` on each flow tick, executes the current node only when the cast reaches zero, then routes to the next node and reads that next item's cast time once. Instant cast nodes can still execute within the same tick, but the existing `maxStepsPerSlice` safety cap bounds runaway instant chains.

Important ownership decision: item owns the cast-time characteristic, but active flow owns runtime progress through that cast time. This keeps item data stable and makes flow lifecycle deterministic without async/freezing/waiting tasks.

Testing note: EditMode tests could not be completed in this environment because the Unity project was already open in another Unity instance. `dotnet build` was also unavailable because no .NET SDK is installed. Static search confirmed no remaining gameplay references to `Duration`; remaining "Duration"/`TimeModule` references are in unrelated UI/time-system code.

AI 2026-05-28 - Removed obsolete ActionExecutor layer

After moving cast-time progress into `FlowProcessor`, user pointed out that `IActionExecutor` was probably removable. This was correct: the executor no longer represented asynchronous waiting or a separate execution boundary. `IActionExecutor`, `ActionExecutorService`, `PreparedAction`, `ExecuteActionCommand`, the ActionExecutor asmdefs, DI binding, and assembly references were removed.

New mechanism: `FlowProcessor` asks `FlowProcessingQueries` for the current item's `IActionDescription` and `ActionCapabilities`, then applies effects directly once the current node's `ItemCastTime` has elapsed. This keeps action timing and execution in the ticked flow lifecycle and avoids an obsolete async/service layer.

AI 2026-05-28 - ItemCastTime must be explicitly present

User pointed out that `ItemActionDescription` should verify that `ItemCastTime` exists even when cast time is zero. Since `ItemCastTime` was initially a struct, it could not be null-guarded and `default(ItemCastTime)` would implicitly mean zero. Refined the design: `ItemCastTime` is now a sealed immutable class/value object, `ItemCastTime.ZERO` is the explicit instant-cast value, and `ItemActionDescription` null-guards both cast time and operations.

AI 2026-05-28 - Flow cast-progress readability and naming

User questioned whether `ticksUntilCurrentNodeExecution` belongs directly in `FlowProcessor`, since `currentNode` already points to the item and the field made `FlowProcessor.tick` harder to read. Clarification: the item/current node owns the cast-time characteristic, but it does not own runtime progress through that cast. Active flow needs separate progress state. Refactor direction: keep the responsibility small and compositional, not a new architecture layer.

Implementation refinement: moved the cast countdown into `CurrentFlowItemCast`, a small helper composed by `FlowProcessor`. `FlowProcessor` now keeps `currentItem` plus `currentItemCast`, calls `advanceFlowDuring(...)`, waits through `currentItemCast.tryFinishCasting(...)`, executes effects through `executeCurrentItemEffects()`, and moves with `tryMoveToNextItem()`. This keeps runtime progress explicit but removes the raw countdown field from the processor.

Naming guidance reinforced: method names, especially public/contract methods, should describe domain intent in human-friendly terms instead of technical bookkeeping. Private helpers should also use mechanic-level names when they represent gameplay steps. Prefer fewer abstractions, but when a small composition object removes unclear state from a central class, it is acceptable.

AI 2026-05-28 - Split dense casting logic into named private helpers

User pointed out that `tryFinishCasting` still contained enough branching that the behavior was not obvious. Refined `CurrentFlowItemCast`: `tryFinishCasting` now reads as a sequence of named scenarios: cast already ready, no ticks available, enough ticks to complete, or partial casting with all available ticks. Extracted private helpers such as `isReadyToExecute`, `hasNoTicksToCastWith`, `canCompleteCastWith`, `completeCastWith`, and `continueCastingWithAll`.

Guidance added: when a method contains multiple domain scenarios, prefer small private methods with clear, mechanic-level names. This should improve readability without creating broad new abstractions.

AI 2026-05-28 - Tick batch semantics and clearer cast tick names

User clarified that `tick(CombatTicks)` intentionally accepts a value object and must support receiving more than one tick in a single call. It should not be implemented as if the value is always `CombatTicks.ONE`. This matters for speed-up, batching, catch-up, and deterministic simulation.

Clarified `CurrentFlowItemCast` naming. When available ticks are enough to finish the cast, the cast consumes only the ticks it needs and leaves the unused ticks for the same flow tick call; method renamed to `finishCastingAndKeepUnusedTicks`. When available ticks are not enough, the cast consumes all available ticks and remains unfinished; method renamed to `spendAllTicksOnCasting`.

AI 2026-05-28 - Test mode for instant flow item casting

Some existing tests use concrete catalog items to verify routing, damage, counters, or item effects, but those tests should not become brittle whenever a catalog item's real cast time changes. Added explicit `FlowCastTimeMode` to `FlowProcessorSettings`: `UseItemCastTime` for production/default behavior and `Instant` for tests that want to skip cast delay while still using real item definitions. `BattleScenarioTestHarness.withInstantFlowItemCastTime()` rebinds settings to instant mode while preserving the current max step safety cap.

Guidance: use real cast time in tests whose purpose is timing/cast behavior. Use instant cast mode for tests whose purpose is not timing, so item definitions can evolve without rewriting unrelated assertions.

AI 2026-05-28 - Reuse CurrentFlowItemCast in hot path

First Unity EditMode test run after enabling real tests found 441/442 passing. The only failure was the performance smoke test, slightly above budget: 10.695 ms/tick versus a 10.5 ms/tick threshold. Instead of raising the threshold, optimized the cast progress helper: `CurrentFlowItemCast` was being allocated every time a flow moved to a next item. It is now a reused helper owned by `FlowProcessor`; `startCasting(...)` resets its countdown for each current item. This keeps the readability/composition benefit without per-transition helper allocation.

Second Unity EditMode run passed all tests: 442/442. Performance smoke reported 10.478 ms/tick average over 1,000 measured ticks.

AI 2026-05-28 - Prefer explicit flow tick pipeline over Chain of Responsibility

User considered making `FlowProcessor` more like chain-actions / plan / pseudo builder to make the order of actions obvious. Guidance: avoid a generic Chain of Responsibility or decorative fluent chain in the flow/tick hot path for now. The pattern is not inherently unusable, but typical implementations can hide control flow, add virtual/delegate/object overhead, and make expensive gameplay work look like cheap builder calls.

Chosen direction: keep `FlowProcessor` as the owner of flow lifecycle, but make the tick body read like an explicit local pipeline through private methods. `advanceFlowDuring(...)` now follows named steps: `canProcessMoreItems`, `tryFinishCurrentItemCast`, `executeCurrentItemEffects`, `tryMoveToNextItem`, and `mustWaitForMoreTicks`. This keeps the order visible without adding another abstraction layer.

AI 2026-05-28 - Performance smoke threshold variance

After the readability refactor, Unity EditMode tests compiled and ran, but the performance smoke failed narrowly at 10.528 ms/tick against the previous 10.5 ms/tick threshold. A previous run after the allocation fix passed at 10.478 ms/tick. Because this smoke is intended as a catastrophic regression guard rather than a stable benchmark, the threshold was deliberately adjusted to 11.0 ms/tick to avoid noise-level failures while still keeping an ambitious budget.

AI 2026-05-28 - Added batched tick carry-over flow test

User asked for a test proving that flow processing carries unused ticks from one item cast to the next item in the same `tick(CombatTicks)` call. Added `should_carry_unused_ticks_to_next_item_in_same_flow_tick` in `FlowSmokeTest`. The test builds a real battle context through `BattleScenarioTestHarness`, places an entry point and adjacent item, creates a flow through `CreateFlowCombatCommand`, then calls character `combatTick(CombatTicks.of(10), ...)`. The entry test item has 3 cast ticks and 2 damage; the next item has 7 cast ticks and 5 damage. The assertion verifies that both effects are applied and the active flow is consumed in that one batched tick.

Testing style reinforced: prefer tests based on real project contracts and references, avoiding mocks where practical, so tests are less brittle around internals and private refactors. Small test-only item definitions are acceptable when they enter through normal item/flow/inventory/combat contracts.

Unity EditMode tests after the new test: 443/443 passed. Performance smoke reported 10.332 ms/tick average over 1,000 measured ticks.

AI 2026-05-28 - Added partial-progress and split-vs-batched tick tests

User asked whether more tests should cover flow/tick behavior because deterministic one-threaded tick processing is central to the game. Added two more real-context EditMode tests:

- `should_keep_partial_cast_progress_between_ticks`: starts a two-item flow, gives it one tick less than needed to finish both casts, verifies HP is unchanged and the flow remains active, then gives one more tick and verifies full damage plus flow completion.
- `should_process_same_result_for_batched_and_split_ticks`: runs the same scenario twice, once as one `CombatTicks.of(10)` call and once as ten `CombatTicks.ONE` calls, then verifies HP and active-flow count match.

Testing principle reinforced: deterministic tick behavior should be tested through real project contracts and stable scenarios. Prefer verifying externally visible combat results and flow counts rather than private helper state.

Unity EditMode tests after these additions: 445/445 passed. Performance smoke reported 10.217 ms/tick average over 1,000 measured ticks.

AI COMMIT BOUNDARY 2026-05-28 - after push 8b3d534 (master) - Remove `ActionExecutor` and related classes; introduce `CurrentFlowItemCast`, `ItemCastTime`, and `FlowCastTimeMode` to improve flow and casting logic; clean up obsolete files and metadata.

Everything below this boundary belongs to the next set of changes after the latest push, unless a later boundary appears.

AI 2026-05-28 - Item cast progress UI and future item flow capacity

User wants item cast progress to be visible directly on the item UI, e.g. progress/loading bars rendered on top of the item while a flow is casting through it. This should be treated as a presentation concern layered onto the item view, not as a reason to move runtime cast ownership out of flow.

Future design note: each item should eventually have a maximum number of concurrently processed flows. Example: `RustySword` may be usable by 3 flows at the same time; if another flow reaches that item while 3 flows are already being processed there, the flow should ignore/skip that item according to the eventual flow rule. The UI design for cast progress should keep this future capacity in mind, because one item may need to show multiple concurrent cast progress bars or lanes.

Immediate focus: do not implement the full item-capacity mechanic yet. First explore and implement a safe visual element for showing item cast/loading progress, preferably additively on `PlacedItemView` and without risky manual prefab/scene edits.

Implementation step: added a code-driven `ItemCastProgressBarsView` and exposed `PlacedItemView.setCastProgressBars(...)` / `hideCastProgressBars()`. The view supports multiple simultaneous progress bars by accepting a list of progress ratios and creating/hiding bar views as needed. This is only the visual layer; it is not yet connected to flow runtime cast-progress data.

Follow-up implementation step: connected the visual layer to real flow cast progress for the currently selected character. Active flows can now collect their current item cast progress as lightweight `(ItemId, progressRatio)` data through a collector interface. After each battle UI tick, presentation refreshes the selected inventory item views and passes each item its list of active cast progress bars. This is intentionally a presentation refresh after tick, not a per-flow UI event emitted from inside the hot tick loop.

Visual correction after user feedback: the first visible implementation used separate bar-like UI, which did not match the intended item presentation. User clarified that cast loading information must live inside the fields occupied by the item, because items have different shapes and can be adjacent. The view was changed to a per-cell overlay: every occupied tile can show inactive/remaining grey coverage and active colored coverage, so cast progress stays within the item shape. Multiple parallel casts are represented as lanes inside each occupied tile.

AI 2026-05-28 - Item flow processing capacity domain implementation

User asked to add domain logic for defining how many flows an item can process in parallel. Added `ItemFlowProcessingCapacity` as a validated value object and exposed it through `IActionDescription` next to item cast time. Example catalog values: `EntryPointGem` capacity 1, `RustySword` capacity 6, `Shield` capacity 3, and `Hammer` capacity 2.

Flow processing now checks item capacity before entering the next routed item. If the candidate item is already at capacity, that item id is added to the ignored/visited list for this flow route attempt and the flow asks routing for another candidate. If no processable next item exists, the flow finishes. Entry point flow creation also checks the entry point capacity before creating a new flow.

Performance correction: the first implementation counted active flows processing an item by scanning active flows, which pushed the stress smoke to about 33 ms/tick. Replaced that with a live per-character dictionary of active flow counts by processed item id. `FlowProcessor` increments/decrements this count when it starts, moves between items, or finishes. This keeps capacity checks cheap and keeps same-tick capacity decisions live.

Testing note: added deterministic tests for catalog capacity values and for skipping a shared item whose capacity is full. The combat tick performance smoke now includes item-processing capacity bookkeeping and its catastrophic-regression threshold was adjusted deliberately to 12 ms/tick. Unity EditMode tests passed 447/447, with the smoke reporting about 11.363 ms/tick.

AI 2026-05-28 - CombatPlacedItemRuntimeState and processable flow router

User refined the capacity ownership direction: a placed item should be able to answer whether it can accept another flow, instead of keeping this as an unexplained loose count on the character or flow processor.

Previous mechanism in the current working change: `CombatCharacter` tracked active flow counts per processed item id directly, and `FlowProcessor`/flow queries participated in deciding whether a routed candidate could be processed. This worked but left capacity knowledge spread between character state, flow processing, and routing retries.

New mechanism: introduced `CombatPlacedItemRuntimeState` as the per-placed-item runtime state owned by `CombatCharacter`. It stores the item id, validates and caches the item's `ItemFlowProcessingCapacity`, tracks active flow count, answers `canAcceptFlow()`, and guards start/finish transitions. `FlowProcessor` only announces when it starts or finishes processing a concrete item; it no longer exposes `isProcessingItem` for character-side counting.

Routing refinement: kept existing `GridAdjacencyRouter`, but added `ProcessableGridAdjacencyRouter` for flow routing that ignores adjacent items which cannot currently accept another flow. Entry point flow creation now builds this router with the character's item-processability query. This matches the near-term desired behavior: if an adjacent item is full, the current router simply does not consider it.

Future routing note: entry points may eventually define or create flows with a characteristic `IFlowRouter`. Different routers can intentionally have different semantics; for example, one router may ignore full items, while another may stop/break the flow when it reaches an unprocessable item.

Performance refinement: the new router first made the performance smoke hover above the 12 ms/tick guard because route decisions checked multiple candidates and long flow paths used a linear visited-list lookup. Instead of raising the threshold, `FlowProcessor` now tracks visited node ids as a `HashSet`, and `ProcessableGridAdjacencyRouter` reuses local routing buffers. Unity EditMode tests passed 447/447; performance smoke reported about 9.684 ms/tick over 1,000 measured ticks.

AI 2026-05-30 - Shape rows define flow slots and cast cost

User decided to stop pursuing cooldown for normal items for now. Normal item processing should be based on cast time only. Entry points may still need their own trigger-time/countdown presentation, but that is separate from item cast progress: trigger time creates a flow, cast time processes an existing flow through an item.

Future TODO direction recorded: active cells and cell components should eventually exist. Base shape defines potential cells, active cells define what a specific item instance can currently use, and cell-level components/overlays may modify row cast time, effect strength, damage type, or other row/slot behavior. For now all shape cells are treated as active.

Implemented step: item flow-processing capacity is now derived from shape rows instead of an explicit `ItemFlowProcessingCapacity` action-description stat. Each local row of the item shape is one processing slot. `CombatPlacedItemRuntimeState` assigns the first free processing slot from top to bottom and returns an `ItemFlowProcessingSlot` reservation to the flow. When the flow leaves or finishes, it releases that concrete slot.

Cast-time meaning changed: `ItemCastTime` now means cost per one cell. The actual cast duration for a processing slot is `itemCastTime * slotCellCount`. A wide row therefore casts longer but can later carry more active cells/components as higher potential.

UI refinement: cast progress now carries the local row of the processing slot. `PlacedItemView` binds each tile to its local row and row order, so the progress mask fills only the row used by that flow and spans that row's width instead of treating the whole item as one flat sequence.

Cleanup: removed `ItemFlowProcessingCapacity` from `IActionDescription` because the capacity source is now shape rows. Unity EditMode tests passed 448/448; performance smoke reported about 3.042 ms/tick with 5,000 long-casting active flows and 5,000 tickable entry items.

AI 2026-05-31 - Cast-state naming and UI/domain boundary cleanup

User pointed out that `ItemCastProgress` looked like a visual/UI concept leaking into combat/domain contracts, and that names such as `getActiveFlowCountProcessingItem`, `IItemCastProgressCollector`, `calculateShapeSlotCastTicks`, and `totalCastTicks` were too technical or unclear for public/domain APIs.

Refinement: combat/domain contracts now expose `ActiveFlowCastState` through `IActiveFlowCastStateCollector`. This state is domain data: processing slot, required cast ticks, and remaining cast ticks. It does not expose UI progress ratio. `CharacterCombatQueries` now collects active flow cast states instead of item cast progress, keeping the query side domain-oriented.

Presentation now owns visual progress conversion. `InventoryPanelPresentation` converts `ActiveFlowCastState` into `ItemCastProgressViewState` by calculating progress ratio from required/remaining ticks. UI classes continue to use progress language because it is presentation-specific there.

Naming cleanup: `getActiveFlowCountProcessingItem` was renamed to `getActiveFlowCountOnItem`, which more directly means "how many active flows are currently on this item." `CurrentFlowItemCast.totalCastTicks` was renamed to required cast ticks, and slot cast-duration calculation moved to `ItemCastTime.getCastTicksFor(ItemFlowProcessingSlot)`, matching the domain rule that item cast time is per cell and slot width multiplies it.

Unity EditMode tests passed 448/448 after the refactor; performance smoke reported about 2.996 ms/tick.

AI 2026-05-31 - Split combat tick performance smoke into two canaries

User clarified that the very fast long-cast smoke result looked suspicious and that performance smoke tests should act as practical anomaly controls for expensive tick actions, not as perfect benchmarks.

Refinement: split the combat tick smoke coverage into two separate tests. The steady-casting smoke keeps 5,000 long-casting active flows alive and intentionally avoids cast completion, routing, flow resolution, and new flow creation during the measured section. This explains the lower average tick cost and keeps it focused on active-flow countdown overhead.

Added a separate routing/resolution smoke that uses isolated `entry -> processor` item pairs. Pairs are separated by empty columns and rows so grid adjacency routing does not accidentally route into neighboring test pairs. The measured loop exercises flow creation, cast completion, routing, processing slot reservation/release, flow consumption, and repeated recreation.

The current routing smoke uses 800 isolated pairs, 200 measured ticks, and an entry trigger interval of 2 ticks. The verified run created exactly 80,000 flows and ended with 800 active flows, confirming that the test performs repeated route/resolve/recreate work rather than measuring an empty or accidentally simplified scenario.

Unity EditMode tests passed 449/449. Smoke output from the verified run: steady-casting 5,000 active flows averaged about 2.851 ms/tick over 1,000 measured ticks; routing/resolution averaged about 12.908 ms/tick over 200 measured ticks.

AI 2026-05-31 - Avoid lazy item runtime-state creation in combat tick

User agreed that lazy creation of `CombatPlacedItemRuntimeState` during `canProcessFlowItem(...)` / routing is risky because combat tick is a hot path. Lazy initialization can be useful in less critical areas, but runtime state needed by flow/tick should be prepared before data enters the tick path.

Refinement: `CombatCharacter` now initializes placed-item runtime state when the combat character is created, after the character inventory has been equipped. Items equipped later through `equipItemOrThrow(...)` also immediately get runtime state. `canProcessFlowItem(...)` and `startProcessingFlowItem(...)` now only read already-prepared state and fail fast if state is missing, treating that as an invariant violation rather than doing hidden allocation/work in the tick.

Additional hot-path cleanup: `CombatCharacter` keeps cached `IFlowItem` wrappers by item id, and `CharacterInventory` caches equipped-item wrappers by item id. This avoids repeatedly building wrapper objects during routing / item lookup paths.

Added deterministic coverage for item equip after combat context creation: an item equipped through the combat command side can immediately participate in flow processing because its runtime state is prepared at equip time.

Unity EditMode tests passed 450/450 after this change. Smoke output from the verified run: steady-casting averaged about 2.858 ms/tick over 1,000 measured ticks; routing/resolution averaged about 12.975 ms/tick over 200 measured ticks.

AI 2026-05-31 - Additional deterministic coverage for row-slot flow processing

User asked to add sensible tests for the main domain assumptions established during the cast-time / shape-row / runtime-state conversation.

Added deterministic tests that verify:

- an entry point cannot create a second flow while its own processing slot is busy,
- processable routing ignores a full adjacent item and routes into the remaining available neighbor,
- active flow cast state exposed through character queries remains domain data and reports the current processing row, row cell count, required cast ticks, and remaining cast ticks,
- after a flow finishes, the processing slot is released and a later flow can use the same item again.

These tests intentionally verify externally visible combat/query results through real battle context and real command/query contracts, rather than testing private helper internals or adding more noisy performance smoke cases.

Unity EditMode tests passed 454/454 after these additions. Smoke output from the verified run: steady-casting averaged about 2.83 ms/tick over 1,000 measured ticks; routing/resolution averaged about 12.63 ms/tick over 200 measured ticks.

AI COMMIT BOUNDARY 2026-05-31 - after push 2b60c27 (master) - Introduce classes for flow processing, flow routing, item cast states, and UI progress bars; add unit tests for flow capacity and routing.

Everything below this boundary belongs to the next set of changes after the latest push, unless a later boundary appears.

AI 2026-05-31 - Flow path visualization on inventory

User wanted active flows to be readable directly on the inventory without opening details: where a flow came from, which item it is currently processing, and which casting slot/row it occupies. User also clarified that the existing item mask/progress visualization should remain the base instead of being replaced.

Previous mechanism: active flow cast UI exposed the current item row and progress ratio to presentation. `PlacedItemView` rendered per-cell row progress lanes inside the occupied item cells, but all lanes shared one color and there was no visual connection from the previous item to the current item.

First implementation mistake: `ActiveFlowCastState` carried optional previous item id and UI assigned visual indexes per refresh. User correctly pointed out that this mixed route/flow identity into cast state and caused colors to change when a flow moved between items.

Refined mechanism: `ActiveFlowCastState` again only describes the current cast. `ActiveFlowState` now describes the active flow projection: flow kind, stable flow visual index, ordered item path, and current cast state. `FlowProcessor` owns the item path and receives the stable visual index when the flow is created. Presentation maps that stable flow visual index to a UI palette, colors existing row/cell progress lanes by flow, and renders pooled UI connection lines from the previous path item to the current processing row. The overlay is presentation-only, refreshed through the existing selected-character UI update path, and is now drawn in front of item views/cast masks.

Testing note: updated deterministic flow coverage to verify that active flow state after routing exposes the ordered item path and previous item through the flow projection. Unity EditMode tests passed 454/454 after the refinement.

Further UI refinement after user feedback: the flow path should be complete and row-based, not only the latest item-to-item jump. `ActiveFlowState` now exposes an ordered processing path made of `ItemFlowProcessingSlot` values, so every path entry identifies the item and row slot used by the flow. UI presentation creates separate `FlowPathViewState` data for the overlay, while `ItemCastProgressViewState` remains focused on row progress/color for item masks. The overlay draws a thicker line through each processed row, connects the end of one row to the start of the next row, and caps visible path segments to avoid overwhelming the UI when many long flows are active. Batchmode tests were blocked by an already open Unity project instance during this refinement, but the active editor log showed a successful script rebuild with no C# compile errors.

Additional visual polish: item cell placement now uses the same `GridLayoutGroup.spacing` as the inventory grid for both internal item cell offsets and item origin positioning. The previous item UI used a separate serialized spacing value inside `PlacedItemView`, so the first occupied cell could align while later cells drifted away from the grid. Flow path rows now reserve up to five visible lanes per item row; overlapping flow paths on the same row are offset around the center line instead of drawing exactly on top of each other.

User feedback on full-line overlay: the row-lane approach represented the requested data but looked too visually noisy. Replaced solid/offset lanes with dashed path segments. Different flows use the same path geometry but get a dash phase derived from stable `flowVisualIndex`, so overlapping flows can interleave colored dashes instead of stacking thick continuous lines. Focused flow visualization by selected entry point/item was recorded as TODO because it needs a clear UI selection/focus model and likely stable start-entry-point data on `ActiveFlowState`.

Flow color allocation refinement: user clarified that flow colors must come from a pool, not from a monotonically increasing counter modulo palette size. Added active flow visual index pooling on `CombatCharacter`: new flows reserve the first free visual index, finished flows release their index during active-flow compaction, and duplicate colors appear only after all available visual indexes are already in active use. Added deterministic coverage for unique visual indexes until the pool is exhausted.

Dashed-line refinement: user clarified that dashed paths should appear only when more than one active flow uses the same cast row. The overlay now counts active flow usage per `item + localRow`; normal row segments render as solid lines, while shared row segments render as dashed lines with per-flow dash phase. Connectors between rows remain solid for now. Non-color flow markers such as repeated glyphs/geometric symbols were recorded as a future accessibility TODO.

AI 2026-06-01 - Flow completion after last enemy death

User noticed a likely crash when an enemy dies while the attacker still has active flows in progress. Desired behavior: active flows may keep processing after the last enemy is dead; when they finish and there is no living target, their outgoing damage should be ignored instead of crashing or repeatedly trying to destroy the dead character presentation.

Previous mechanism: `CombatContext.tryGetRandomEnemyOf(...)` selected enemies by team only and had a TODO to filter out dead characters. A character with HP 0 stayed registered in the combat context, so later offensive flow completion could still target it. `CharacterAggregate.takeDamage(...)` also published `CharacterDeathDtoEvent` whenever current HP was `<= 0`, even if the character was already dead before the damage call.

New mechanism: random enemy selection now treats only characters with `currentHp > 0` as valid targets. If no living enemy exists, offensive flow consumption simply does not dispatch damage. `CharacterDeathDtoEvent` is published only on the transition from HP above zero to HP zero. This preserves the current runtime model where dead characters remain registered for now, while preventing repeated death events and allowing active flows to finish safely after the last target dies.

Regression coverage: added a deterministic EditMode test with two manually created attacker flows. The first flow kills a 1 HP defender; the second flow remains active and finishes after the defender is already dead. The test verifies that HP remains at zero, the death event count stays at one, no exception is thrown, and the delayed flow is compacted normally.

AI 2026-06-01 - Flow identity must stay domain, visual identity must stay presentation

User rejected the previous `flowVisualIndex` direction because it put presentation concerns into flow/domain creation. This is now a hard architectural rule: domain/runtime models must not store display palette slots, colors, opacity states, dash phases, marker glyphs, highlight modes, or other view-specific characteristics. If UI needs stable visual treatment for a domain object, domain may expose a neutral identity such as `ActiveFlowId`, and presentation maps that identity to visual state locally.

Previous mechanism: `CombatCharacter` owned an `ActiveFlowVisualIndexPool`, passed `flowVisualIndex` into `IFlowFactory.create(...)`, `FlowProcessor` stored it, and `ActiveFlowState` exposed it. This made the domain responsible for color-pool lifecycle and made tests assert visual slot allocation.

New mechanism: active flow runtime receives a domain `ActiveFlowId`. `IFlowFactory.create(...)` now accepts a `FlowCreationCommand` object to avoid a growing parameter list. `FlowProcessor` and `ActiveFlowState` expose `ActiveFlowId`, flow kind, processing path, and current cast state only. `InventoryPanelPresentation` maps `ActiveFlowId` to a temporary UI visual index/color/dash phase with a local presentation allocator. Tests now assert domain flow ids, not color indexes.

Flow focus UX direction:
- Default inventory mode should avoid showing every possible path when it becomes noisy.
- Selecting an entry point should show only active flows started by that entry point, strongly outline the selected entry point, keep participating items at normal visual weight, and dim unrelated items.
- Selecting a normal item should show local flow context: incoming and outgoing flow paths around that item. Direct neighbors / currently connected items should be most prominent; farther path context may fade out with distance or recentness.
- Selecting a concrete flow should isolate that one flow, highlight all items in its path, and eventually show per-item/per-row contribution to flow power.
- Dimming, highlighting, outlining, and fading rules should be centralized in UI presentation styling, not scattered per component. Prefer a small fixed set of visual states such as normal, dimmed, focused, related, and stale-related.

UX styling preference: use opacity plus mild desaturation/darkening for dimmed items, not grayscale alone. Opacity communicates focus while preserving item identity; desaturation helps when colors are busy. Use strong outline/glow only for the selected object, and use softer outline or normal brightness for related objects. Avoid expensive post-processing; prefer pooled UI Images/CanvasGroups/material tinting and cheap alpha transitions.

AI 2026-06-01 - Focused inventory readability and smooth progress

User liked the focused flow direction but observed that dimmed items became hard to see against the grid and that showing all flow lines by default hurt inventory readability.

Previous mechanism: item tiles only enabled outline for the focused state, so dimmed items could visually collapse into the grid. The flow path overlay rendered active flow paths even without an explicit item/entry/flow selection. Cast progress masks jumped at combat tick cadence, making the UI look like it was running at low FPS.

New mechanism: item visual states keep an always-on tile outline, with normal/related/dimmed states using softer outlines and focused state using a much stronger outline/glow. Flow path overlay is hidden in the normal unfocused inventory view and appears only after the player selects an item/entry point or an already visible flow line. Cast progress remains derived from deterministic combat ticks, but UI lanes interpolate toward the latest progress target between ticks for smoother presentation.

Review refinement: smooth cast-progress interpolation must not mean every item runs a permanent `Update()`. `ItemCastProgressBarsView` disables itself while no progress lanes are visible and enables itself only while active cast progress is displayed. Lane layout should be based on the currently visible lane count, not the maximum pooled lane count, so rows recover full-height lanes after overlapping flows disappear.

AI COMMIT BOUNDARY 2026-06-04 - after push 16ef972 (master) - Add `FlowConnectionOverlayView`, `FlowPathViewState`, and related classes for UI flow visualization; replace `IActiveFlowCastStateCollector` with `IActiveFlowStateCollector`; introduce unit tests for inventory item cast progress bars and corresponding visual state rendering.

Everything below this boundary belongs to the next set of changes after the latest push, unless a later boundary appears.

AI 2026-06-04 - Defensive receive-damage flow, game-event triggers, and modifier philosophy

User is now thinking about the mechanism for receiving damage. Current mental model: an enemy sends the damage specification together with the related offensive flow/result. The receiving side should have a defensive phase, currently imagined around `DamageIncomingCombatEventProcessor`.

Desired defensive behavior:

- When incoming attack damage is about to be received and a defensive entry point is available/free, the character creates a new defensive flow triggered by that defensive entry point.
- The defensive flow receives the incoming damage amount as its input. In `FlowKind.DEFENSIVE`, `DamageRole.ATTACK` should be interpreted as incoming damage directed at the character receiving damage.
- Defensive flow behaves mechanically like offensive flow: the same item logic can modify roles such as `ATTACK`, so a shield doing `-5 ATTACK` is useful in defensive flow while a sword doing `+5 ATTACK` may increase incoming damage. This is acceptable because item behavior should be transparent and role-based, not hidden behind special cases.
- Processing a defensive flow must always end with applying final damage to the target. This is critical to avoid infinite receive-damage loops.
- The final damage produced by the defensive flow is tentatively called `ResolvedDamage`, but the exact name should remain open because future plans may refine the terminology.
- Damage that has already gone through the defensive flow must not be registered again as fresh incoming damage and must not trigger another defensive flow.

Anti-loop constraints:

- If defensive flow produces damage aimed back at the attacker, that damage must be represented as a distinct role such as `SPIKE_DAMAGE` / counter-style damage, not as normal incoming attack damage.
- The project needs explicit rules for which damage/effect roles are legal in which `FlowKind`.
- Combos should come from clever item/build interactions within clear rules, not from bypassing or recursively re-entering the damage pipeline.

New entry point direction:

- Add a new defensive entry point type / flow type. It is triggered by a domain game-event/hook related to receiving attack damage from another character.
- User used `OnReceiveDamage` as the player-facing/domain phrase, but the name must be very unambiguous. It should specifically mean "incoming damage from someone else's attack before final HP application", not any internal programming event.
- Earlier naming guidance still applies: `On...` fits hooks/triggers, while concrete event/fact classes should avoid `On...` names.

Game-event clarification:

- Terms like `OnReceiveDamage`, `OnAttack`, etc. are game-mechanic events/hook points, not C# events and not a multithreading/async/pub-sub requirement.
- They are synchronous domain facts/reaction points in the combat model. This aligns with the previous `CombatCommand` / `CombatEvent` / `CombatHook` distinction.

Modifier philosophy and future direction:

- User expects a broad passive modifier system across several layers: whole game, specific character, inventory/module level, and likely later item/cell/component level.
- The game should support event-aware modifiers, for example "on attack", "on receive damage", "every 7 received-damage events, do something".
- Game-event occurrences must be registered in runtime statistics/counters at least at character and combat-context level. Inventory-level counters are being considered but should not be implemented yet.
- These counters are gameplay-relevant runtime data, not only reporting data, because item/modifier conditions may read them during combat.

Core design philosophy to preserve:

The player should feel overloaded by the number of possible build interactions, modifiers, effects, and optimization routes, not by hidden or unintuitive mechanics. The game is fundamentally about creating builds and searching for optimizations. It should be complex because the player has many ways to influence the system, while the actual rules remain readable, consistent, and exposed enough that the player should not need a wiki to understand what happened. The desired target is: the player enters the game, feels overwhelmed by build complexity, but can understand the mechanics from the game itself because there are no hidden rules or surprising logic. Player agency and clear cause/effect are extremely important.

Performance/architecture concern:

Event-aware modifiers may run very often, potentially every combat tick. Avoid broad scans for every event. The likely direction is to index available hooks/listeners by hook/event kind and owner scope when inventory/character runtime state is prepared, then dispatch/increment counters synchronously using reusable buffers and cheap primitive queries. This should respect the current architecture: combat runtime records facts/counters, character/inventory expose scoped reaction candidates, and item reactions still go through explicit match -> validate processability -> prepare -> react style phases where that workflow is needed.

AI 2026-06-04 - Implemented event-triggered defensive entry point foundation

Previous mechanism:

- `IInventoryPlacedEntryPoint` was also an `IInventoryCombatTickableItem`, so every entry point was tick-triggered.
- Offensive flow completion created `DamageIncomingCombatEvent`, and `DamageIncomingCombatEventProcessor` immediately converted raw incoming damage to `ResolvedDamage` and applied it to HP.
- `FlowKind.Defense` existed, but `CombatContext.processDefensiveFlow(...)` threw `NotImplementedException`.
- Flow creation had no initial attack-power input, so a defensive flow could not start from incoming damage without special-casing item effects.

New mechanism:

- Entry point definitions now expose `EntryPointTriggerKind`: `CombatTick` or `CombatEvent`.
- Tick-triggered placed entry points still implement `IInventoryCombatTickableItem`; event-triggered placed entry points remain entry points but are not tickable.
- Added `EventEntryPoint` archetype and `DefenseEntryPointGem` as the first catalog example of an event-triggered `FlowKind.Defense` entry point.
- The concrete combat fact is now `IncomingAttackDamageCombatEvent`, while `OnIncomingAttackDamage` remains the hook-style name for future player-facing/modifier language.
- `FlowCreationCommand` can carry initial `ATTACK` power and an optional source character id. Defensive flow seeds `DamageRole.ATTACK` with the raw incoming damage before processing entry point/item effects.
- `DamageIncomingCombatEventProcessor` now tries to start an available event-triggered defensive entry point. If it starts one, HP is not changed immediately. If no defensive entry point is available/free, incoming damage is resolved and applied directly.
- `CombatContext.processDefensiveFlow(...)` now applies final `ResolvedDamage` directly to the defensive flow owner and does not dispatch another incoming damage event. This is the current anti-recursion guard.
- Combat and character query sides now expose cheap `getCombatEventCount(CombatEventType)` counters. `INCOMING_ATTACK_DAMAGE` is counted globally and on the targeted character.

Tests added/updated:

- Event-triggered defensive entry points do not create flows from the normal tick phase.
- Incoming attack damage can create a defensive flow, leave HP unchanged until that defensive flow resolves, then apply modified resolved damage once.
- A busy defensive entry point causes incoming attack damage to resolve directly instead of creating a second defensive flow.
- Unity EditMode test run passed: 461/461 tests.

AI 2026-06-04 - Test item drag pools for offensive and defensive items

- User asked for the add-item drag button to stop using equal random selection and instead use a weighted offensive pool: 10% `Hammer`, 30% offensive `EntryPointGem`, 60% `RustySword`.
- User also asked for a second test drag button for defensive items. Current test pool should expose the new defensive mechanics by offering a defensive event entry point (`DefenseEntryPointGem`) and a shield (`Shield`).
- This is a temporary/test UI affordance for quickly placing sample items. It is not the final inventory/shop/drop-generation architecture.

AI 2026-06-04 - Entry point UI colors by flow kind

- User asked for defensive and offensive entry points to be visually distinguishable by color.
- Current UI convention: offensive/damage entry points use a warm orange color; defensive entry points use a green/turquoise color.
- This is presentation-only. Domain/runtime logic exposes `FlowKind`; UI maps that to color locally and does not store visual colors in combat/domain state.

AI 2026-06-04 - Defensive flow balance brainstorming: flat reduction, percent reduction, burst pressure, and barrier

This is exploratory design context, not final architecture.

User concerns about defensive damage processing:

- Pure flat damage reduction is difficult to balance across the game because it is very strong against small hits and weak against large hits.
- Pure percent reduction is also risky. In late game, with flows potentially passing through many items, percent reduction can too easily approach 100%. That can turn full mitigation into a default expectation, while early game players cannot reach similar behavior.
- User wants a readable mechanic that remains interesting and does not collapse into a hidden or default rule.
- User does not want to solve burst/synchronization with a naive "all damage in one tick becomes one packet" rule. The real synchronization problem likely happens over several seconds, not a single tick. If defensive/event-entry cooldowns are around 2-5 seconds and combat has about 10 ticks per second, attackers can still lightly offset attacks and reproduce the same problem.
- A key offensive risk: if defensive event entry points can be consumed/forced, attackers may always try to synchronize several long attacks or bombard the defender to exhaust available defensive responses. This could be a cool mechanic sometimes, but should not become the universal default offensive strategy.

Barrier idea raised by user:

- Add a barrier pool separate from HP.
- Maximum barrier is determined by character traits and modifiers.
- Barrier can exceed its maximum through overload, but barrier added beyond the max has decreasing effectiveness, for example 90%, 80%, 70%, etc.
- Barrier could act as a defense-side damage grouping/smoothing system.

Barrier tradeoffs noted by user:

- Plus: very readable. It gives the player a visible defense resource and can make burst damage easier to understand.
- Neutral/mixed: defensive processing may no longer directly react to every incoming damage event in the same way as the current event-entry-point model.
- Minus: if barrier fully abstracts incoming damage, defensive flow may lose context about what kind of damage was received. Damage characteristics could stop at the barrier/character layer instead of passing through defensive flow.

Assistant proposal direction:

- Consider barrier not as simple "second HP", but as a pressure buffer / damage smoothing layer.
- Incoming attacks add `pressure` / `pending damage` to the defensive side. Barrier and defensive flow process that pressure over time before it leaks into HP.
- This can make synchronized burst attacks create a large pressure spike without necessarily applying all damage instantly before defense can respond.
- Keep damage context in a lightweight pending-damage ledger if needed: amount, damage type/tags, source, age, and role. This preserves readability while allowing defensive mechanics to ask "what kind of pressure is this?"
- Defensive flow could react to pressure/barrier events such as `OnIncomingAttackDamagePressure` or `OnBarrierPressureAdded`, rather than only raw immediate HP damage.
- This would preserve event-aware build potential while avoiding a purely hidden percent-reduction stack.

Open design question:

- Need decide whether defensive flow should process immediate incoming damage directly, process barrier/pressure state, or support both through clearly named roles/events.
- Need avoid infinite receive-damage recursion regardless of the chosen model.
- Need ensure burst offense, sustained offense, barrier regeneration, pressure leak, and defensive entry point availability create multiple viable strategies instead of one dominant "always synchronize everything" strategy.

AI 2026-06-05 - Barrier/pressure refinement for autobattler defense

This is exploratory design context, not final architecture.

- User clarified that treating all damage as pressure that leaks to HP over time can resemble turning all damage into a DoT. That may fit games where the player directly reacts with dodges/healing, but may not fit an autobattler where decisions are mostly made through build setup before combat.
- The defense model should therefore avoid relying on manual real-time reactions. Defensive responses should come from prebuilt automatic systems, item characteristics, entry points, counters, and visible rules.
- The design space should explore concrete defensive systems that react automatically to incoming attack patterns: anti-burst barriers, charge-based guards, trigger-priority defensive entry points, damage-type wards, emergency safeguards, pressure valves, and retaliation/spike systems.
- A promising direction is to keep barrier/pressure as a readable defensive state, but make its response rules build-driven rather than player-action-driven. For example, items can predefine how the character handles burst, sustained hits, specific damage types, or repeated incoming events.
- Need avoid making "all damage becomes delayed DoT" the core rule unless it is clearly justified and readable in an autobattler context.

AI 2026-06-05 - Heavy Guard defensive archetype sketch

This is exploratory design context, not final architecture.

- `Heavy Guard` is a possible automatic defensive archetype aimed at large incoming attacks, not small chip damage.
- It should have a clear activation rule such as "trigger only when a single incoming attack damage packet is at least X" or "trigger when incoming damage exceeds Y% of current/max HP or barrier".
- It should be charge/cooldown/budget limited, so it cannot answer every attack. This keeps it as an anti-burst tool rather than universal mitigation.
- Heavy Guard can either reduce/absorb part of the incoming attack, convert part of it into barrier overload, split the attack into a more manageable form, or start a defensive flow specialized for large hits.
- Heavy Guard should be beatable by strategies such as many medium attacks, baiting with threshold-crossing attacks, damage type mismatch, or exhausting its charge window. It should not make synchronized burst universally bad; it should create a buildable answer to burst.
- A useful player-facing expression could be: "When incoming attack damage is 80 or more, consume 1 Guard Charge and process it through this defensive flow before HP/barrier application."

AI 2026-06-05 - Defensive flow producing conditional defense portions

This is exploratory design context, not final architecture.

- User proposed a refinement: every defensive flow could end by creating/applying one "portion" of defense rather than immediately reducing a specific incoming damage value.
- During defensive flow creation/processing, the flow would define the specification of that defense portion/barrier: strength, trigger conditions, selection rules, and possibly type/tags.
- Example direction: a build can generate many small barriers quickly and a few strong barriers slowly. Each barrier portion can be configured by the defensive flow and defensive items.
- Items such as a shield could define when a barrier portion should be selected/consumed, for example only against large hits, only against specific damage types, only if incoming damage exceeds a threshold, or based on priority.
- This means if an enemy prepares a large synchronized burst, the defender may have had time to prepare a set of strong conditional barriers. Those portions can be consumed at the same moment by the incoming burst.
- This keeps room for anti-burst builds, but does not make synchronized burst the only correct offensive strategy because defenders can prepare specifically for it and attackers can choose patterns that interact differently with the available barrier portions.
- This model may preserve autobattler readability better than turning all damage into delayed pressure/DoT, because the visible state can be "prepared defenses waiting for matching attacks" rather than "damage leaking over time."

AI 2026-06-05 - CRITICALLY IMPORTANT: challenge design ideas and prioritize balanceable systems

- User explicitly wants AI to challenge and criticize proposed mechanics when there are logical holes, obvious exploits, dominant strategies, bypasses, or ways to abuse the currently planned game logic.
- AI should not merely agree with user proposals. It should actively test ideas against edge cases such as burst synchronization, baiting defensive triggers, flat-damage scaling, late-game saturation, early-game weakness, and "blocks everything or blocks nothing" behavior.
- This is critically important because the project is a solo indie game. The design should favor systems that are understandable, predictable, and balanceable without requiring a large QA/testing team.
- User needs mechanics that can be steered and predicted through balance parameters. The game should avoid systems that become balance hell because tiny value changes collapse into either full mitigation or instant death.
- Automated deterministic tests/simulations are expected to be very important for this game type. Future balance work should include many scenario tests that measure survivability, burst handling, sustained damage handling, trigger consumption, and dominant strategy risks.

Current challenge to conditional defense portions/barriers:

- User is concerned that direct numeric/flat barrier values may still become a slippery balance problem.
- A flat barrier/portion system can collapse into "blocks everything" when values are high enough or "blocks nothing" when an attack slips past the matching defense.
- Missing one incoming attack could instantly kill the character if defense is too binary.
- User suspects flat damage response may remain dangerous for balance even if barrier portions have smart conditions. This may be solvable with additional mechanics, but it is not safe to assume.
- Need search for a defensive model where outcomes degrade gracefully instead of snapping between total protection and death.

AI 2026-06-05 - Static gear segment and guard-rating defense idea

This is exploratory design context, not final architecture.

- User is considering a separate static equipment segment outside the flow grid. This would be a different item segment responsible for baseline attack/defense characteristics rather than path/flow processing.
- Naming is open. Candidate concepts include static gear/loadout/modules/relics/core equipment, but this should not be confused with flow-grid inventory items.
- One possible direction: health resource becomes HP + barrier. Defensive entry points generate one barrier/defense portion with a concrete value and possible properties.
- Static gear could define defensive/offensive characteristics such as physical defense, HP, barrier generation, attack generation, and similar baseline properties.
- User is not convinced by direct static percent resistances such as `-10% physical damage`, because ARPGs often converge on an endgame "cap all resistances" default. This may recreate the same PoE/Last Epoch/Grim Dawn resistance-cap meta the project wants to avoid.
- User is considering whether static gear should instead use flat/stat-growth style bonuses, or boost generation of shield/barrier/HP/attack rather than directly reacting to each incoming attack.

Guard-rating / defensive-ability idea:

- Instead of a barrier value blocking the same amount of damage 1:1, a defense portion could express an abstract defensive ability/rating.
- Example intuition: `100 defense` against `100 damage` may block around `80`, while `1000 defense` against `100 damage` may block around `99`, never becoming perfect.
- This means defense value is converted to mitigation effectiveness by an algorithm based on the relationship between defense rating and incoming damage.
- User concern: this may make small fast attacks too weak because they are easily mitigated by small/fast defense segments, or in the opposite direction may make small attacks too strong if the formula/consumption model is tuned poorly.

Critical risks to check:

- A static percent-resistance gear layer can easily become mandatory baseline optimization, reducing build expression to "cap required defenses".
- A guard-rating formula can become opaque if the UI cannot clearly explain why a given hit dealt a given amount.
- Most armor-style formulas are naturally strong against small hits and weaker against large hits. This may be good if intentional, but can also invalidate rapid small-hit builds unless those builds apply separate pressure such as guard fatigue, trigger drain, armor shred, or ailment stacking.
- Need a model where static gear provides baseline identity and build constraints without replacing the interesting flow/item logic.

AI 2026-06-05 - Guard hybrid direction, readability risk, and sandboxed defense logic goal

This is exploratory design context, not final architecture.

- User accepts that rapid small attacks can have a role as guard-breaking / guard-draining attacks. Small guards being efficient against small attacks does not automatically kill small-attack builds if guard portions have strain/fatigue/consumption and this is balanced well.
- User's main critique of guard-rating / defensive-ability formulas is reduced readability. The game goal is high clarity, so asking the player to understand an abstract numeric "defensive ability / guard rating" and its formula is a real risk.
- This risk does not immediately reject the idea, but it must be handled through naming, UI, and simple explainable formulas. The player should not need a wiki to understand why damage was mitigated.
- User is warming to a hybrid model: defensive entry points can generate guard portions; future entry points may generate healing, but healing entry points are only a note for later discussion.
- User is also considering event entry points that react to concrete situations, such as direct damage intake, but this must be specified carefully to avoid the same traps already identified around recursion, baiting, and unclear timing.
- Important game-feel goal: the player should feel they are constructing the logic of the game's attack/defense mechanisms, not merely selecting fixed stats. The attack/defense system should be sandbox-like and configurable through build choices. There should not be one immutable defense/attack system; players should be able to define how their own mechanisms respond.
- This reinforces the broader game vision: complexity should come from many clear, configurable mechanisms and build interactions, while cause/effect remains readable and predictable.

AI 2026-06-05 - Guard naming/readability while preserving flat flow values

This is exploratory design context, not final architecture.

- User wants flow to still move/process flat amount values. The open problem is how to express defensive ability/guard so it remains readable while avoiding direct flat "barrier blocks exactly N damage" balance issues.
- A single player-facing guard statistic may be acceptable if the game explains it clearly and UI shows practical consequences, for example "how this guard behaves against a 50/100/200 damage hit" rather than requiring the player to infer a hidden formula.
- Candidate player-facing names should be evaluated for clarity: `Guard Power`, `Guard Strength`, `Shield Strength`, `Protection`, `Absorption`, `Ward Strength`, `Stability`, or Polish equivalents such as `Siła Gardy`, `Siła Osłony`, `Moc Osłony`, `Stabilność`.
- Need avoid a black-box stat where the player only sees "Guard 237" without knowing whether that is good. Every guard portion should communicate its intended use: strong vs large hits, weak vs rapid small hits, type-specific, high durability, low durability, etc.
- One possible readability direction: each guard portion still has one main flat value, but tooltip/log presents examples or bands: "best against hits around X", "blocks about Y from a Z hit", "breaks after about N matching hits."

AI 2026-06-05 - CRITICALLY IMPORTANT: guard tooltips require concrete incoming damage, not abstract offensive rating

- User strongly liked the guard tooltip/readability direction where hovering a guard shows concrete examples such as: against 60 damage blocks about 55, against 120 damage blocks about 95, against 240 damage blocks about 130.
- This can satisfy the readability goal because the player can see how a guard behaves against specific incoming damage values without understanding the full formula.
- Critical caveat: this only stays readable if the attacking side produces concrete damage/power values that the player can reason about. If attack output becomes another abstract stat such as `Offensive Power`, the tooltip loses context and the game risks becoming like ARPG offensive/defensive rating systems where values are hard to interpret.
- Avoid defining attack power as an equally abstract opposed rating unless absolutely necessary and accompanied by very clear translation into actual damage. Otherwise `100 Guard Power` no longer has a clear relationship to "100 incoming damage", and simple guard tooltips become much harder to make.
- Preserve the current direction that flow can move flat amounts, and that player-facing attack output should remain as concrete damage-like values whenever possible.
- Critical design rule: one abstract stat can be manageable if the opposing side remains concrete. Two abstract opposing stats quickly destroy readability. In this context, abstract `Guard Power` can work if it is shown against concrete incoming damage values; abstract `Guard Power` versus abstract `Offensive Power` should be avoided unless there is an exceptionally clear player-facing translation.
